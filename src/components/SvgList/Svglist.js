import React from 'react';

import {Image} from 'react-native'
export const HomeSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return (
   <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const LivetvSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 25;
  let width = scale * 40;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const VideoSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const PlaySVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const CircleplaySVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const SearchSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const SendSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};

export const MenuSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 100;
  let width = scale * 100;
  let fillColor = props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};



 const NewsNationSVG = props => {
  let scale = props.scale ? props.scale : 1;
  let height = scale * 170;
  let width = scale * 280;
  let fillColor= props.fill ? props.fill : '#000';
  return(
    <Image source={{uri:'https://img.icons8.com/color/48/null/monzo.png'}} />
  );
};



export default NewsNationSVG;

