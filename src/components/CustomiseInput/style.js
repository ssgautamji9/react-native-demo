import { StyleSheet } from "react-native";
import themeColors from "../../utils/themeColors";

const styles = StyleSheet.create({
    inputtxt: {
        backgroundColor: themeColors.inputColor.theme,
        marginHorizontal: 30,
    }

});

export {styles}